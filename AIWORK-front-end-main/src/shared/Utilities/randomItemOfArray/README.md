# Overview

The `randomItemOfArray` function returns a random item from the given array.

# Examples

```typescript
import { randomItemOfArray } from "path-to-randomItemOfArray-file";

const array = [1, 2, 3, 4, 5];
const result = getRandomItem(array);
console.log(result); // Output: A random item from the array
```
